services:
  postgres:
    image: postgres:18.4
    environment:
      - POSTGRES_DB=petclinic
      - POSTGRES_USER=petclinic
      - POSTGRES_PASSWORD=petclinic
    volumes:
      - petclinic-postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U petclinic -d petclinic"]
      interval: 5s
      timeout: 5s
      retries: 5

  ms-vets-owners:
    image: ghcr.io/umbertoleone/spring-petclinic-ms-vets-owners:staging
    pull_policy: always
    environment:
      - SPRING_PROFILES_ACTIVE=postgres
      - POSTGRES_URL=jdbc:postgresql://postgres:5432/petclinic
      - POSTGRES_USER=petclinic
      - POSTGRES_PASS=petclinic
    depends_on:
      postgres:
        condition: service_healthy

  ms-visits:
    image: ghcr.io/umbertoleone/spring-petclinic-ms-visits:staging
    pull_policy: always
    environment:
      - SPRING_PROFILES_ACTIVE=postgres
      - POSTGRES_URL=jdbc:postgresql://postgres:5432/petclinic
      - POSTGRES_USER=petclinic
      - POSTGRES_PASS=petclinic
    depends_on:
      postgres:
        condition: service_healthy
      ms-vets-owners:
        condition: service_started

  ms-core-ui:
    image: ghcr.io/umbertoleone/spring-petclinic-ms-core-ui:staging
    pull_policy: always
    environment:
      - SPRING_PROFILES_ACTIVE=postgres
      - POSTGRES_URL=jdbc:postgresql://postgres:5432/petclinic
      - POSTGRES_USER=petclinic
      - POSTGRES_PASS=petclinic
      - MS_VETS_OWNERS_URL=http://ms-vets-owners:8081
      - MS_VISITS_URL=http://ms-visits:8082
    depends_on:
      - ms-vets-owners
      - ms-visits

  api-gateway:
    image: ghcr.io/umbertoleone/spring-petclinic-api-gateway:staging
    pull_policy: always
    ports:
      - "8086:8080"
    environment:
      - MS_VETS_OWNERS_URL=http://ms-vets-owners:8081
      - MS_VISITS_URL=http://ms-visits:8082
      - MS_CORE_UI_URL=http://ms-core-ui:8083
    depends_on:
      - ms-core-ui

  prometheus:
    image: ghcr.io/umbertoleone/spring-petclinic-prometheus:staging
    pull_policy: always
    container_name: prometheus-staging
    restart: always
    volumes:
      - petclinic-prometheus-data:/prometheus

  grafana:
    image: ghcr.io/umbertoleone/spring-petclinic-grafana:staging
    pull_policy: always
    container_name: grafana-staging
    restart: always
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH=/etc/grafana/provisioning/dashboards/jvm-micrometer.json
    volumes:
      - petclinic-grafana-data:/var/lib/grafana
    depends_on:
      - prometheus

  loki:
    image: ghcr.io/umbertoleone/spring-petclinic-loki:staging
    pull_policy: always
    container_name: loki-staging
    user: "0:0"
    restart: always
    volumes:
      - petclinic-loki-data:/tmp/loki

  promtail:
    image: ghcr.io/umbertoleone/spring-petclinic-promtail:staging
    pull_policy: always
    container_name: promtail-staging
    user: "0:0"
    restart: always
    environment:
      - PETCLINIC_ENV=staging
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
    depends_on:
      - loki

volumes:
  petclinic-postgres-data:
  petclinic-prometheus-data:
  petclinic-grafana-data:
  petclinic-loki-data:
